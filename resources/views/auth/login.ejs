<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css">
    <script src="/js/import/alpine.js" type="text/javascript" defer></script>
    <title>LOGIN</title>
</head>

<body class="min-h-sceen">
    <div class="w-full min-h-screen flex flex-col items-center justify-center">
        <div class="w-full max-w-xs">
            <form class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4" method="post">
                <div class="mb-4">
                    <label class="block text-gray-700 text-sm font-bold mb-2" for="email">
                        Email
                    </label>
                    <input <% if(errors && errors.email) { %>
                    class="input-text input-error"
                    <% } else { %>
                        class="input-text"
                        <% } %>
                            id="email"
                            type="text"
                            placeholder="Email"
                            />
                            <p class="text-red-500 text-xs italic">
                                <% if(errors && errors.email) return errors.email.msg %>
                            </p>
                </div>
                <div class="mb-6">
                    <label class="block text-gray-700 text-sm font-bold mb-2" for="password">
                        Password
                    </label>
                    <input <% if(errors && errors.password) { %>
                    class="input-text input-error"
                    <% } else { %>
                        class="input-text"
                        <% } %>
                            id="password"
                            type="password"
                            placeholder="******************"
                            autocomplete="current-password"
                            />
                            <p class="text-red-500 text-xs italic">
                                <% if(errors && errors.password) return errors.password.msg %>
                            </p>
                </div>
                <div class="flex items-center justify-between">
                    <button
                        class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                        type="submit">
                        Sign In
                    </button>
                    <a class="inline-block align-baseline font-bold text-sm text-blue-500 hover:text-blue-800" href="#">
                        Forgot Password?
                    </a>
                </div>
            </form>
            <p class="text-center text-gray-500 text-xs">
                &copy;2020-2024 Tooltorch. All rights reserved.
            </p>
        </div>
    </div>
</body>

</html>