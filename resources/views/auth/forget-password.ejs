<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css">
    <script src="/js/import/alpine.js" defer></script>
    <title>Forgot Password</title>
</head>

<body class="min-h-sceen">
    <div class="w-full min-h-screen flex flex-col items-center justify-center">
        <span class="text-xl text-gray-500 font-medium mb-10">Forgot Password</span>
        <div class="w-full max-w-sm">
            <form class="bg-white rounded px-8 pt-6 pb-8 mb-4" method="post">
                <div class="mb-4">
                    <h1 class="text-gray-600 text-xs font-regular mb-5">Enter the email address associated with your account and we'll send you a link to reset your password</h1>
                    <label class="block text-gray-800 text-sm font-normal mb-2" for="email">
                        Email
                    </label>
                    <input <% if(errors && errors.email) { %>
                    class="input-text input-error"
                    <% } else { %>
                        class="input-text"
                        <% } %>
                        value="<%= errors && errors.email ? formData?.email : '' %>"
                            id="email"
                            type="text"
                            name="email"
                            
                            autocomplete="off"
                            />
                            <p class="text-red-500 text-xs italic">
                                <%= errors?.email?.msg %>
                            </p>
                </div>
                
                <div class="flex items-center justify-between">
                    <button
                        class="w-full inline-flex justify-center whitespace-nowrap rounded-lg bg-blue-500 px-3 py-2.5 text-sm font-medium text-white shadow-sm shadow-blue-950/10 hover:bg-blue-600 focus:outline-none focus:ring focus:ring-blue-300 focus-visible:outline-none focus-visible:ring focus-visible:ring-blue-300 transition-colors duration-150"
                        type="submit">
                        Continue
                    </button>
                </div>
                <p class="text-xs text-gray-500 flex gap-1 mt-4">
                    Don't have an account?
                    <a class="text-blue-400 hover:underline" href="/register">Sign Up here</a>
                </p>
            </form>
            <%- include("../components/copyright-form-text.ejs") %>
        </div>
    </div>
</body>

</html>